diff --git a/node_modules/@sveltejs/kit/dist/cli.js b/node_modules/@sveltejs/kit/dist/cli.js
index bf0b1e8..0d0e91a 100644
--- a/node_modules/@sveltejs/kit/dist/cli.js
+++ b/node_modules/@sveltejs/kit/dist/cli.js
@@ -551,11 +551,11 @@ const options = object(
 					assert_string(input, keypath);
 
 					if (input) {
-						if (!/^[a-z]+:\/\//.test(input)) {
-							throw new Error(
-								`${keypath} option must be an absolute path, if specified. See https://kit.svelte.dev/docs#configuration-paths`
-							);
-						}
+						// if (!/^[a-z]+:\/\//.test(input)) {
+						// 	throw new Error(
+						// 		`${keypath} option must be an absolute path, if specified. See https://kit.svelte.dev/docs#configuration-paths`
+						// 	);
+						// }
 
 						if (input.endsWith('/')) {
 							throw new Error(
